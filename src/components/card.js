import React from 'react';
import styled from 'styled-components';

const PrimaryGreen = '#7AA59A';

const Button = styled.button`
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    padding: 4.5px;
    background-color:green;
`;

export default Button;

